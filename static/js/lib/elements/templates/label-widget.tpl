<label>{{ field_label }}</label>
<ul class="{{field_name}} tags editor">

  <li class="{{ field_name }} is-new">
  <input type="text" placeholder="{{ field_label }}">
  </li>
</ul>
<select multiple="true" name="{{field_name}}" class="hidden">
</select>

